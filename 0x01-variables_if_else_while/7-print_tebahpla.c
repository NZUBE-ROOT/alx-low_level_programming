#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int low;

	for (d = 'z'; d <= 'a' d++)
	putchar(low);
	putchar('\n');

	return (0);
}
